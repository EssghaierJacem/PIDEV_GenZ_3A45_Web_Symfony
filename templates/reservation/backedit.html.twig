<!DOCTYPE html>

<title>Edit Reservation</title>

{% block body %}


    {{ include('reservation/backform.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('reservation_index') }}">back to list</a>

    {{ include('reservation/_delete_form.html.twig') }}
{% endblock %}
