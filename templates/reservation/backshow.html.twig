{% extends 'backbase.html.twig' %}
{% block body %}




        <section class="breadcrumbs">
         <div class="container">

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservation.id }}</td>
            </tr>
            <tr>
                <th>Nom_client</th>
                <td>{{ reservation.nomClient }}</td>
            </tr>
            <tr>
                <th>Prenom_client</th>
                <td>{{ reservation.prenomClient }}</td>
            </tr>
            <tr>
                <th>Num_tel</th>
                <td>{{ reservation.numTel }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ reservation.quantite }}</td>
            </tr>
            <tr>
                <th>Date_reservation</th>
                <td>{{ reservation.dateReservation ? reservation.dateReservation|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Qr_code</th>
                <td>{{ reservation.qrCode }}</td>
            </tr>
        </tbody>
    </table>
        <section class="breadcrumbs">
    <div class="container">
        <div class="d-flex justify-content-between align-items-center">
            <h2>Reservation Statistics</h2>
            <ol>
               
                <li><a href="{{ path('app_reservation_index') }}">Reservations</a></li>
                <li>Statistics</li>
            </ol>
        </div>
    </div>
</section>

<section class="statistics">
    <div class="container">
     
        {# Add more statistics here if needed #}
    </div>
</section>

<section class="buttons">
    <div class="container">
        <a href="{{ path('reservation_statistics') }}" class="btn btn-primary">View Statistics</a>
    </div>
</section>
    <div class="text-end mt-4">
    <a href="{{ path('reservation_index') }}" class="btn btn-sm btn-info">back to list</a>
    </div>

    <a href="{{ path('back_edit', {'id': reservation.id}) }}" class="btn btn-sm btn-warning">edit</a>

     {{ include('reservation/_delete_form.html.twig') }}
    </section>
{% endblock %}

