{% extends 'backbase.html.twig' %}

{% block body %}
<section class="breadcrumbs">
    <div class="container">
        <div class="d-flex justify-content-between align-items-center">

        </div>
    </div>
</section>
  <section class="breadcrumbs">
         <div class="container">

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande.id }}</td>
            </tr>
            <tr>
                <th>Num_commande</th>
                <td>{{ commande.numCommande }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ commande.prix }}</td>
            </tr>
            <tr>
                <th>Code_promo</th>
                <td>{{ commande.codePromo }}</td>
            </tr>
            <tr>
                <th>Type_paiement</th>
                <td>{{ commande.typePaiement }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ commande.email }}</td>
            </tr>
            <tr>
                <th>Date_commande</th>
                <td>{{ commande.dateCommande ? commande.dateCommande|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>
 <div class="text-end mt-4">
    <a href="{{  path('commande_index') }}" class="btn btn-sm btn-info">back to list</a>
    </div>

    

     {{ include('commande/_delete_form.html.twig') }}
    </section>
{% endblock %}
