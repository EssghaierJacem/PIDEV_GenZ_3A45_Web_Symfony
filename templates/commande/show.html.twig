{% extends 'frontbase.html.twig' %}

{% block body %}
<section class="breadcrumbs">
    <div class="container">
        <div class="d-flex justify-content-between align-items-center">

        </div>
    </div>
</section>
<section class="breadcrumbs">
    <div class="container">

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ commande.id }}</td>
                </tr>
                <tr>
                    <th>Num_commande</th>
                    <td>{{ commande.numCommande }}</td>
                </tr>
                <tr>
                    <th>Code_promo</th>
                    <td>{{ commande.codePromo }}</td>
                </tr>
                <tr>
                    <th>Type_paiement</th>
                    <td>{{ commande.typePaiement }}</td>
                </tr>
                <tr>
                    <th>Email</th>
                    <td>{{ commande.email }}</td>
                </tr>
                <tr>
                    <th>Date_commande</th>
                    <td>{{ commande.dateCommande ? commande.dateCommande|date('Y-m-d') : '' }}</td>
                </tr>
            </tbody>
        </table>
        <div class="text-end mt-4">
            <a href="{{ path('app_commande_index') }}" class="btn btn-sm btn-info">Back to list</a>
        </div>

        <a href="{{ path('app_commande_edit', {'id': commande.id}) }}" class="btn btn-sm btn-warning">Edit</a>

        {{ include('commande/_delete_form.html.twig') }}

        <!-- Add PDF button here -->
        <a href="{{ path('generate_pdf', {'id': commande.id}) }}" class="btn btn-sm btn-primary">Generate PDF</a>

    </div>
</section>
{% endblock %}
