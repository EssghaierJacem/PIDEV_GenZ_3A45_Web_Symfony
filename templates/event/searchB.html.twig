{% extends 'backbase.html.twig' %}

{% block body %}
    <div class="midde_cont">
                      <div class="container-fluid">
                         <div class="row column_title">
                            <div class="col-md-12">
                             <div class="page_title">
                                <h2><a href="{{ path('app_event_index') }}"  ">
                                 <i class="fa fa-arrow-left">   &nbsp; </i>
                                </a> Event </h2>
                            </div>
                      </div>
                    </div>

    <section class="breadcrumbs">
        <div class="container">
            <h1>Recherche de Event</h1>

            {# Affichage du formulaire de recherche #}
            {{ form_start(form) }}
            {{ form_row(form.nom, {'attr': {'class': 'form-control mb-3', 'placeholder': 'Nom'}}) }}
            {{ form_row(form.dateDebut, {'attr': {'class': 'form-control mb-3', 'placeholder': 'Date de début'}}) }}
            {{ form_row(form.lieu, {'attr': {'class': 'form-control mb-3', 'placeholder': 'Lieu'}}) }}
            {# Ajoutez d'autres champs de recherche si nécessaire #}

            <button type="submit" class="btn btn-primary">Rechercher</button>
            {{ form_end(form) }}
        </div>
    </section>
{% endblock %}
